{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Messenger window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
